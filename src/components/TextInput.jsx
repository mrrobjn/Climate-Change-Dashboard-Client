import '../assets/scss/components/TextInput.scss'
const TextInput = ({placeholder}) => {
  return <input className='input-text' type="search" placeholder={placeholder} />;
};

export default TextInput;
